node_modules/
dist/
.env
.env.local
*.log
.DS_Store
.idea/
.vscode/
coverage/
infra/vps/logs/
backups/

# Raw OULAD dataset — re-downloaded on demand via `npm run data:download`
# (figshare DOI 10.6084/m9.figshare.5081998, CC BY 4.0). 500 MB+, not for git.
data/oulad/anonymisedData.zip
data/oulad/*.csv

# ── Internal — NOT for the public repo / hackathon jury ──────────────
# Private team Telegram export (messages + member photos) — whole tree.
docs/telegram/
# Cross-session AI working notes: candid dev notes, infra/host details,
# rejected approaches. Kept locally, never published.
docs/CLAUDE_NOTES.md
# Local env override sometimes used by the Vite dev server.
frontend/.env
.env.*
!.env.example
