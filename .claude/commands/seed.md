---
description: Run all seed files
allowed-tools: Bash(npm run seed:run:*)
---

```
cd backend && npm run seed:run
```

Show the result. If there are no seed files, tell the user: "No seed files in `backend/seeds/`, create one via `/seed-new`."
